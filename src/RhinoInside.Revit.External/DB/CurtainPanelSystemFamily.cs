namespace RhinoInside.Revit.External.DB
{
  // Revit API does not have an enum for this (eirannejad: 2020-04-15)
  // replace with Revit API enum when implemented
  public enum CurtainPanelSystemFamily
  {
    Unknown = -1,
    Empty = 0,
    Solid = 1,
  }
}
